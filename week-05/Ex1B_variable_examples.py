customer_id = 1001  # I assumed this is a number, but it could also be a string if IDs include letters (e.g., "C1001")

customer_name = "Hana Tilahun"  # I used full name in one variable, but it might be better to split into first_name and last_name

customer_gender = "Female"  # I used a string, but it may need a standardized set of values or include more options

customer_date_of_birth = "1998-05-12"  # I used a string in YYYY-MM-DD format, but using a date type would be better for calculations

drivers_license_number = "D1234567"  # I assumed it's a string because it can include letters and numbers

auto_policy_number = "AP987654321"  # I used a string, assuming policy numbers may include letters and should not be treated as 

#If you want to confirm your work
#Add a few prints at the bottom:

print(customer_id)
print(customer_name)
print(customer_gender)

my_name = "Hana Tilahun"

city = "Silver Spring"
state = "Maryland"

#Lab 2 answers
# a) Full list of Python reserved keywords (cannot be used as variable names):
# False, None, True, and, as, assert, async, await, break, class, continue,
# def, del, elif, else, except, finally, for, from, global, if, import, in,
# is, lambda, nonlocal, not, or, pass, raise, return, try, while, with, yield

# b) 5 keyword explanations:

# def: used to define a function in Python.
# Example: def my_function():

# for: used to create a loop that iterates over a sequence (like a list or string).
# Example: for item in list:

# if: used for conditional statements to run code only if a condition is true.
# Example: if x > 5:

# return: used inside a function to send a value back to where the function was called.
# Example: return result

# class: used to define a ^^blueprint^^ for creating objects (^^object-oriented programming^^).
# Example: class Car:
